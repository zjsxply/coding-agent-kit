# Copy to .env, fill values, then: set -a; source .env; set +a
# Some agents cache env vars into config files. If you change env vars later, rerun `cakit configure <agent>` when applicable.

# Shared (optional)
# CAKIT_OUTPUT_DIR=
# CAKIT_TRAE_TRAJECTORY=
# CAKIT_NPM_PREFIX=

# Claude Code
# Config: OAuth via `claude` then `/login`, or API via ANTHROPIC_API_KEY. Claude Code reads env vars directly; `cakit configure claude` is a no-op.
# ANTHROPIC_API_KEY=
# ANTHROPIC_BASE_URL=
# ANTHROPIC_AUTH_TOKEN=
# CAKIT_CLAUDE_USE_OAUTH=1   # cakit-only switch
# ANTHROPIC_MODEL=
# ANTHROPIC_DEFAULT_OPUS_MODEL=
# ANTHROPIC_DEFAULT_SONNET_MODEL=
# ANTHROPIC_DEFAULT_HAIKU_MODEL=
# CLAUDE_CODE_SUBAGENT_MODEL=
# CLAUDE_CODE_ENABLE_TELEMETRY=1
# OTEL_EXPORTER_OTLP_ENDPOINT=

# OpenAI Codex
# Config: API mode uses CODEX_API_KEY. For OAuth, set CAKIT_CODEX_USE_OAUTH=1 and run `codex login`
# (writes $CODEX_HOME/auth.json), then run `cakit configure codex`.
# CODEX_API_KEY=
# CAKIT_CODEX_USE_OAUTH=1   # cakit-only switch
# CODEX_API_BASE=https://api.openai.com/v1
# CODEX_MODEL=gpt-5.2-codex
# CODEX_OTEL_EXPORTER=otlp-http
# CODEX_OTEL_ENDPOINT=http://localhost:4318
# CODEX_OTEL_PROTOCOL=
# CODEX_OTEL_ENVIRONMENT=
# CODEX_OTEL_LOG_USER_PROMPT=false

# Cursor Agent
# Config: OAuth via `cursor-agent login`, or API via CURSOR_API_KEY.
# CURSOR_API_KEY=
# CURSOR_API_BASE=
# CURSOR_MODEL=

# GitHub Copilot CLI
# Config: OAuth via `copilot` then `/login`, or GitHub token auth via GH_TOKEN/GITHUB_TOKEN.
# GH_TOKEN=        # requires Copilot Requests permission
# GITHUB_TOKEN=    # requires Copilot Requests permission
# COPILOT_MODEL=

# Gemini CLI
# Config: OAuth via `gemini` login, or API via GEMINI_API_KEY / GOOGLE_API_KEY.
# GEMINI_API_KEY=
# GOOGLE_API_KEY=
# GOOGLE_GEMINI_BASE_URL=
# GOOGLE_CLOUD_PROJECT=
# GEMINI_MODEL=

# Crush
# Config: OAuth via `crush login` (e.g. `crush login hyper`), or API via CRUSH_OPENAI_API_KEY/CRUSH_OPENAI_BASE_URL.
# CRUSH_OPENAI_API_KEY=
# CRUSH_OPENAI_BASE_URL=
# CAKIT_CRUSH_MODEL=   # cakit-only default model for `cakit run crush` API mode

# Auggie
# Config: OAuth via `auggie login`, or API via AUGMENT_API_TOKEN/AUGMENT_API_URL.
# AUGMENT_API_URL must be the Augment tenant URL (for example `https://your-tenant.api.augmentcode.com/`),
# not a generic OpenAI-compatible `/v1` endpoint.
# AUGMENT_API_TOKEN=
# AUGMENT_API_URL=
# AUGMENT_SESSION_AUTH=   # optional OAuth session JSON (same format as ~/.augment/session.json)
# GITHUB_API_TOKEN=
# CAKIT_AUGGIE_MODEL=   # cakit-only default model for `cakit run auggie`

# Continue CLI
# Config: OAuth via `cn login`, or API via CAKIT_CONTINUE_OPENAI_API_KEY/CAKIT_CONTINUE_OPENAI_BASE_URL/CAKIT_CONTINUE_OPENAI_MODEL.
# CAKIT_CONTINUE_OPENAI_API_KEY=
# CAKIT_CONTINUE_OPENAI_BASE_URL=
# CAKIT_CONTINUE_OPENAI_MODEL=

# Goose CLI
# Config: API via CAKIT_GOOSE_PROVIDER/CAKIT_GOOSE_MODEL and OpenAI-compatible credentials.
# CAKIT_GOOSE_PROVIDER=openai
# CAKIT_GOOSE_MODEL=
# CAKIT_GOOSE_OPENAI_API_KEY=
# CAKIT_GOOSE_OPENAI_BASE_URL=https://api.openai.com/v1
# CAKIT_GOOSE_OPENAI_BASE_PATH=v1/chat/completions   # optional override

# Kilo Code CLI
# Config: API via KILO_OPENAI_API_KEY/KILO_OPENAI_MODEL_ID. Run `cakit configure kilocode` to write ~/.kilocode/cli/config.json.
# KILO_OPENAI_API_KEY=
# KILO_OPENAI_BASE_URL=
# KILO_OPENAI_MODEL_ID=

# OpenClaw
# Config: API via CAKIT_OPENCLAW_API_KEY/CAKIT_OPENCLAW_BASE_URL/CAKIT_OPENCLAW_MODEL.
# Run `cakit configure openclaw` to initialize ~/.openclaw via `openclaw onboard --non-interactive`.
# CAKIT_OPENCLAW_API_KEY=
# CAKIT_OPENCLAW_BASE_URL=
# CAKIT_OPENCLAW_MODEL=   # supports provider/model or bare model id
# CAKIT_OPENCLAW_PROVIDER_ID=   # optional provider id for OpenClaw custom provider
# CAKIT_OPENCLAW_CONTEXT_WINDOW=32000   # cakit-only minimum contextWindow patched into OpenClaw custom provider model
# CAKIT_OPENCLAW_MAX_TOKENS=32000       # cakit-only minimum maxTokens patched into OpenClaw custom provider model

# Deep Agents CLI (API only)
# Config: API via DEEPAGENTS_OPENAI_API_KEY/DEEPAGENTS_OPENAI_MODEL.
# cakit maps these to upstream OPENAI_API_KEY/OPENAI_BASE_URL for deepagents.
# DEEPAGENTS_OPENAI_MODEL should include provider (for example `openai:gpt-5`).
# If provider is omitted (for example `kimi-k2.5`), cakit rewrites it to `openai:<model>`.
# DEEPAGENTS_OPENAI_API_KEY=
# DEEPAGENTS_OPENAI_BASE_URL=
# DEEPAGENTS_OPENAI_MODEL=

# Kimi Code
# Config: API via KIMI_API_KEY. Run `cakit configure kimi` to write ~/.kimi/config.toml.
# Core vars for cakit Kimi config/run:
# KIMI_API_KEY=
# KIMI_BASE_URL=https://api.moonshot.cn/v1
# KIMI_MODEL_NAME=kimi-k2.5   # optional; used by `cakit run kimi` as `--model`
# CAKIT_KIMI_PROVIDER_TYPE=openai_responses   # allowed: kimi|openai_legacy|openai_responses

# TRAE CLI (trae.cn)
# Config: API via CAKIT_TRAE_CN_API_KEY/CAKIT_TRAE_CN_BASE_URL/CAKIT_TRAE_CN_MODEL. Run `cakit configure trae-cn`.
# CAKIT_TRAE_CN_API_KEY=
# CAKIT_TRAE_CN_BASE_URL=
# CAKIT_TRAE_CN_MODEL=
# CAKIT_TRAE_CN_MODEL_NAME=cakit-openai
# CAKIT_TRAE_CN_BY_AZURE=0

# Qwen Code
# Config: OAuth via `qwen` login, or API via QWEN_OPENAI_API_KEY. Run `cakit configure qwen`.
# QWEN_OPENAI_API_KEY=
# QWEN_OPENAI_BASE_URL=
# QWEN_OPENAI_MODEL=
# TAVILY_API_KEY=
# CAKIT_QWEN_GOOGLE_API_KEY=   # cakit-only Google API key for Qwen
# GOOGLE_SEARCH_ENGINE_ID=

# OpenHands (API only)
# Config: API only via upstream env names. No OAuth.
# LLM_API_KEY=
# LLM_MODEL=          # e.g. openai/gpt-5 or moonshot/kimi-k2.5
# LLM_BASE_URL=
# For OpenHands, cakit normalizes model format for LiteLLM routing:
# - `provider:model` => `provider/model`
# - bare model (for example `kimi-k2.5`) => `openai/<model>`

# SWE-agent (API only)
# Config: API only via SWE_AGENT_API_KEY/SWE_AGENT_API_BASE/SWE_AGENT_MODEL.
# SWE_AGENT_API_KEY=
# SWE_AGENT_API_BASE=
# SWE_AGENT_MODEL=
# CAKIT_SWE_AGENT_VERSION=   # cakit-only install pin
# CAKIT_SWE_AGENT_GITHUB_TOKEN=   # cakit-only token used for SWE-agent release lookup

# Trae OSS (API only)
# Config: API only via TRAE_AGENT_API_KEY/TRAE_AGENT_API_BASE/TRAE_AGENT_MODEL.
# TRAE_AGENT_API_KEY=
# TRAE_AGENT_API_BASE=
# TRAE_AGENT_MODEL=
# CAKIT_TRAE_OSS_COMMIT=   # cakit-only install pin
