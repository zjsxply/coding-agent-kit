# 复制为 .env，填写值后执行：set -a; source .env; set +a
# 部分 agent 会把环境变量缓存到配置文件里。后续若修改环境变量，按需重新执行 `cakit configure <agent>`。

# 共享 OpenAI 兼容回退（可选）
# 对于支持 OpenAI 兼容端点的 coding agent，当 agent 专属 API/base/model 变量未设置时，
# cakit 会把这些共享变量映射到该 agent 对应的环境变量。
# 这些值用于决定该 agent 使用的 OpenAI 兼容提供方端点与模型。
# 这些 agent 的模型优先级：--model > agent 专属模型环境变量 > OPENAI_DEFAULT_MODEL
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_DEFAULT_MODEL=

# cakit 共享运行时设置（可选）
# CAKIT_OUTPUT_DIR=   # cakit 输出目录，用于保存 `raw_output` 日志与生成的 `trajectory_path` 文件（默认：~/.cache/cakit）
# CAKIT_NPM_PREFIX=   # npm 类 coding agent 在 `cakit install ... --scope user` 下的安装前缀（默认：~/.npm-global）

# Claude Code
# 配置：OAuth 方式为运行 `claude` 后输入 `/login`；API 方式为 ANTHROPIC_API_KEY。Claude Code 直接读取环境变量；`cakit configure claude` 是空操作。
# ANTHROPIC_API_KEY=
# ANTHROPIC_BASE_URL=
# ANTHROPIC_AUTH_TOKEN=
# CAKIT_CLAUDE_USE_OAUTH=1   # 仅 cakit 使用的开关
# ANTHROPIC_MODEL=
# ANTHROPIC_DEFAULT_OPUS_MODEL=
# ANTHROPIC_DEFAULT_SONNET_MODEL=
# ANTHROPIC_DEFAULT_HAIKU_MODEL=
# CLAUDE_CODE_SUBAGENT_MODEL=
# CLAUDE_CODE_ENABLE_TELEMETRY=1
# OTEL_EXPORTER_OTLP_ENDPOINT=

# OpenAI Codex
# 配置：API 模式使用 CODEX_API_KEY。OAuth 模式请设置 CAKIT_CODEX_USE_OAUTH=1 并执行 `codex login`
# （会写入 $CODEX_HOME/auth.json），然后执行 `cakit configure codex`。
# CODEX_API_KEY=
# CAKIT_CODEX_USE_OAUTH=1   # 仅 cakit 使用的开关
# CODEX_API_BASE=https://api.openai.com/v1
# CODEX_MODEL=gpt-5.2-codex
# CODEX_OTEL_EXPORTER=otlp-http
# CODEX_OTEL_ENDPOINT=http://localhost:4318
# CODEX_OTEL_PROTOCOL=
# CODEX_OTEL_ENVIRONMENT=
# CODEX_OTEL_LOG_USER_PROMPT=false

# Cursor Agent
# 配置：OAuth 方式为 `cursor-agent login`；API 方式为 CURSOR_API_KEY。
# CURSOR_API_KEY=
# CURSOR_API_BASE=
# CURSOR_MODEL=

# GitHub Copilot CLI
# 配置：OAuth 方式为运行 `copilot` 后输入 `/login`；也支持通过 GH_TOKEN/GITHUB_TOKEN 鉴权。
# GH_TOKEN=        # 需要 Copilot Requests 权限
# GITHUB_TOKEN=    # 需要 Copilot Requests 权限
# COPILOT_MODEL=

# Gemini CLI
# 配置：OAuth 方式为 `gemini` login；API 方式为 GEMINI_API_KEY / GOOGLE_API_KEY。
# GEMINI_API_KEY=
# GOOGLE_API_KEY=
# GOOGLE_GEMINI_BASE_URL=
# GOOGLE_CLOUD_PROJECT=
# GEMINI_MODEL=

# Crush
# 配置：OAuth 方式为 `crush login`（例如 `crush login hyper`）；API 方式为 CRUSH_OPENAI_API_KEY/CRUSH_OPENAI_BASE_URL。
# CRUSH_OPENAI_API_KEY=
# CRUSH_OPENAI_BASE_URL=
# CAKIT_CRUSH_MODEL=   # 仅 cakit 使用的 API 模式默认模型（`cakit run crush`）

# CodeBuddy Code
# 配置：OAuth 方式为 `codebuddy login`；API 方式为 CODEBUDDY_API_KEY。
# 对于中国/iOA 账号使用平台 API key 时，设置 CODEBUDDY_INTERNET_ENVIRONMENT 为 `internal`/`ioa`。
# CODEBUDDY_API_KEY=
# CODEBUDDY_AUTH_TOKEN=
# CODEBUDDY_BASE_URL=
# CODEBUDDY_MODEL=
# CODEBUDDY_INTERNET_ENVIRONMENT=

# Aider（仅 API）
# 配置：仅 API，使用 AIDER_OPENAI_API_KEY/AIDER_MODEL。
# AIDER_OPENAI_API_KEY=
# AIDER_OPENAI_API_BASE=
# AIDER_MODEL=

# OpenCode
# 配置：OAuth 方式为 `opencode auth login`；API 方式为 CAKIT_OPENCODE_OPENAI_API_KEY/CAKIT_OPENCODE_MODEL。
# CAKIT_OPENCODE_OPENAI_API_KEY=
# CAKIT_OPENCODE_OPENAI_BASE_URL=
# CAKIT_OPENCODE_PROVIDER=   # 当 CAKIT_OPENCODE_MODEL 已包含 provider 时可不填；provider 列表可运行 `opencode models`
# CAKIT_OPENCODE_MODEL=   # 支持 provider/model 或 provider:model；若只填裸模型 id，请同时设置 CAKIT_OPENCODE_PROVIDER
# CAKIT_OPENCODE_MODEL_CAPABILITIES=   # 可选：自定义 API 模型支持的输入模态，逗号分隔 text,audio,image,video,pdf（例如 image,video）

# Factory Droid CLI
# 配置：OAuth 方式为运行 `droid` 后输入 `/login`；API 方式为 FACTORY_API_KEY。
# FACTORY_API_KEY=
# FACTORY_API_BASE_URL=   # 可选：上游 API base URL 覆盖（用于支持的部署）
# FACTORY_TOKEN=   # 可选：部分 CI 流程使用的 token 环境变量名
# CAKIT_FACTORY_MODEL=   # 仅 cakit 使用的默认模型（`cakit run factory` 的 `droid exec --model`）
# CAKIT_FACTORY_BYOK_API_KEY=   # 仅 cakit 使用：写入 ~/.factory/settings.json customModels[].apiKey 的上游 key
# CAKIT_FACTORY_BYOK_BASE_URL=  # 仅 cakit 使用：写入 customModels[].baseUrl 的上游 base URL
# CAKIT_FACTORY_BYOK_PROVIDER=  # 仅 cakit 使用：BYOK provider，openai | anthropic | generic-chat-completion-api（可选；未设置时自动推断）
# FACTORY_LOG_FILE=   # 可选：上游 CLI 日志输出路径
# FACTORY_DISABLE_KEYRING=1   # 可选：在无头环境禁用 keyring

# Auggie
# 配置：OAuth 方式为 `auggie login`；API 方式为 AUGMENT_API_TOKEN/AUGMENT_API_URL。
# AUGMENT_API_URL 必须是 Augment 租户 URL（例如 `https://your-tenant.api.augmentcode.com/`），
# 不能是通用 OpenAI 兼容 `/v1` 端点。
# AUGMENT_API_TOKEN=
# AUGMENT_API_URL=
# AUGMENT_SESSION_AUTH=   # 可选 OAuth session JSON（与 ~/.augment/session.json 格式一致）
# GITHUB_API_TOKEN=
# CAKIT_AUGGIE_MODEL=   # 仅 cakit 使用的默认模型（`cakit run auggie`）

# Continue CLI
# 配置：OAuth 方式为 `cn login`；API 方式为 CAKIT_CONTINUE_OPENAI_API_KEY/CAKIT_CONTINUE_OPENAI_BASE_URL/CAKIT_CONTINUE_OPENAI_MODEL。
# CAKIT_CONTINUE_OPENAI_API_KEY=
# CAKIT_CONTINUE_OPENAI_BASE_URL=
# CAKIT_CONTINUE_OPENAI_MODEL=

# Goose CLI
# 配置：API 方式使用 CAKIT_GOOSE_PROVIDER/CAKIT_GOOSE_MODEL 与 OpenAI 兼容鉴权信息。
# CAKIT_GOOSE_PROVIDER=openai
# CAKIT_GOOSE_MODEL=
# CAKIT_GOOSE_OPENAI_API_KEY=
# CAKIT_GOOSE_OPENAI_BASE_URL=https://api.openai.com/v1
# CAKIT_GOOSE_OPENAI_BASE_PATH=v1/chat/completions   # 可选覆盖

# Kilo Code CLI
# 配置：API 方式使用 KILO_OPENAI_API_KEY/KILO_OPENAI_MODEL_ID。运行 `cakit configure kilocode` 写入 ~/.kilocode/cli/config.json。
# KILO_OPENAI_API_KEY=
# KILO_OPENAI_BASE_URL=
# KILO_OPENAI_MODEL_ID=

# OpenClaw
# 配置：API 方式使用 CAKIT_OPENCLAW_API_KEY/CAKIT_OPENCLAW_BASE_URL/CAKIT_OPENCLAW_MODEL。
# 运行 `cakit configure openclaw` 通过 `openclaw onboard --non-interactive` 初始化 ~/.openclaw。
# CAKIT_OPENCLAW_API_KEY=
# CAKIT_OPENCLAW_BASE_URL=
# CAKIT_OPENCLAW_MODEL=   # 支持 provider/model 或裸模型 id
# CAKIT_OPENCLAW_PROVIDER_ID=   # 可选：OpenClaw 自定义 provider id
# CAKIT_OPENCLAW_CONTEXT_WINDOW=32000   # 仅 cakit 使用：写入 OpenClaw 自定义 provider model 的最小 contextWindow
# CAKIT_OPENCLAW_MAX_TOKENS=32000       # 仅 cakit 使用：写入 OpenClaw 自定义 provider model 的最小 maxTokens

# Deep Agents CLI（仅 API）
# 配置：API 方式使用 DEEPAGENTS_OPENAI_API_KEY/DEEPAGENTS_OPENAI_MODEL。
# cakit 会将这些变量映射到 deepagents 上游的 OPENAI_API_KEY/OPENAI_BASE_URL。
# DEEPAGENTS_OPENAI_MODEL 应包含 provider（例如 `openai:gpt-5`）。
# 若省略 provider（例如 `kimi-k2.5`），cakit 会重写为 `openai:<model>`。
# DEEPAGENTS_OPENAI_API_KEY=
# DEEPAGENTS_OPENAI_BASE_URL=
# DEEPAGENTS_OPENAI_MODEL=

# Kimi Code
# 配置：API 方式使用 KIMI_API_KEY。运行 `cakit configure kimi` 写入 ~/.kimi/config.toml。
# cakit Kimi 配置/运行的核心变量：
# KIMI_API_KEY=
# KIMI_BASE_URL=https://api.moonshot.cn/v1
# KIMI_MODEL_NAME=kimi-k2.5   # 可选；若设置了 --model 或 OPENAI_DEFAULT_MODEL 可不填
# CAKIT_KIMI_PROVIDER_TYPE=openai_responses   # 可选值：kimi|openai_legacy|openai_responses

# TRAE CLI（trae.cn）
# 配置：API 方式使用 CAKIT_TRAE_CN_API_KEY/CAKIT_TRAE_CN_BASE_URL/CAKIT_TRAE_CN_MODEL。运行 `cakit configure trae-cn`。
# CAKIT_TRAE_CN_API_KEY=
# CAKIT_TRAE_CN_BASE_URL=
# CAKIT_TRAE_CN_MODEL=
# CAKIT_TRAE_CN_MODEL_NAME=cakit-openai
# CAKIT_TRAE_CN_BY_AZURE=0

# Qwen Code
# 配置：OAuth 方式为 `qwen` login；API 方式为 QWEN_OPENAI_API_KEY。运行 `cakit configure qwen`。
# QWEN_OPENAI_API_KEY=
# QWEN_OPENAI_BASE_URL=
# QWEN_OPENAI_MODEL=
# TAVILY_API_KEY=
# CAKIT_QWEN_GOOGLE_API_KEY=   # 仅 cakit 使用：Qwen 的 Google API key
# GOOGLE_SEARCH_ENGINE_ID=

# Qoder CLI
# 配置：OAuth 方式为 `qodercli /login`；也支持通过 QODER_PERSONAL_ACCESS_TOKEN 鉴权。
# QODER_PERSONAL_ACCESS_TOKEN=
# CAKIT_QODER_MODEL=        # 仅 cakit 使用的默认模型档位，例如 auto|efficient|performance|ultimate
# 注意：qodercli 不支持自定义 OpenAI 兼容 API key/base URL 鉴权。

# OpenHands（仅 API）
# 配置：仅支持上游环境变量名。不支持 OAuth。
# LLM_API_KEY=
# LLM_MODEL=          # 例如 openai/gpt-5 或 moonshot/kimi-k2.5
# LLM_BASE_URL=
# 对 OpenHands，cakit 会为 LiteLLM 路由规范化模型格式：
# - `provider:model` => `provider/model`
# - 裸模型名（例如 `kimi-k2.5`）=> `openai/<model>`

# SWE-agent（仅 API）
# 配置：仅 API，使用 SWE_AGENT_API_KEY/SWE_AGENT_API_BASE/SWE_AGENT_MODEL。
# SWE_AGENT_API_KEY=
# SWE_AGENT_API_BASE=
# SWE_AGENT_MODEL=
# CAKIT_SWE_AGENT_GITHUB_TOKEN=   # 仅 cakit 使用：用于查询 SWE-agent release 的 token

# Trae OSS（仅 API）
# 配置：仅 API，使用 TRAE_AGENT_API_KEY/TRAE_AGENT_API_BASE/TRAE_AGENT_MODEL。
# TRAE_AGENT_API_KEY=
# TRAE_AGENT_API_BASE=
# TRAE_AGENT_MODEL=
# CAKIT_TRAE_TRAJECTORY=   # 仅 cakit 使用：覆盖 `trae-cli run --trajectory-file` 路径（默认：<cwd>/trae_trajectory.json）
