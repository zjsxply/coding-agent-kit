# Copy to .env, fill values, then: set -a; source .env; set +a
# If you change env vars later, rerun `cakit configure <agent>` to refresh configs.

# Shared (optional)
# CAKIT_OUTPUT_DIR=
# CAKIT_TRAE_TRAJECTORY=

# OpenAI Codex
# Config: API mode uses CODEX_API_KEY. For OAuth, set CODEX_USE_OAUTH=1 and run `codex login`
# (writes $CODEX_HOME/auth.json), then run `cakit configure codex`.
# CODEX_API_KEY=
# CODEX_USE_OAUTH=1   # use OAuth login instead of API key
# CODEX_API_BASE=https://api.openai.com/v1
# CODEX_MODEL=gpt-5-codex
# CODEX_OTEL_EXPORTER=otlp-http
# CODEX_OTEL_ENDPOINT=http://localhost:4318
# CODEX_OTEL_PROTOCOL=
# CODEX_OTEL_ENVIRONMENT=
# CODEX_OTEL_LOG_USER_PROMPT=false
# OTEL_EXPORTER_OTLP_ENDPOINT=

# Claude Code
# Config: OAuth via `claude` then `/login`, or API via ANTHROPIC_API_KEY. Run `cakit configure claude` if needed.
# ANTHROPIC_API_KEY=
# ANTHROPIC_BASE_URL=
# ANTHROPIC_AUTH_TOKEN=
# CLAUDE_CODE_MODEL=
# CLAUDE_CODE_ENABLE_TELEMETRY=1
# OTEL_EXPORTER_OTLP_ENDPOINT=

# Gemini CLI
# Config: OAuth via `gemini` login, or API via GEMINI_API_KEY/GOOGLE_API_KEY. Run `cakit configure gemini`.
# GEMINI_API_KEY=
# GOOGLE_API_KEY=
# GOOGLE_GEMINI_BASE_URL=
# GOOGLE_CLOUD_PROJECT=
# GEMINI_MODEL=

# Kimi Code
# Config: API only via KIMI_API_KEY. Run `cakit configure kimi`.
# KIMI_API_KEY=
# KIMI_BASE_URL=https://api.kimi.com/coding/v1
# KIMI_MODEL_NAME=kimi-for-coding

# Qwen Code
# Config: OAuth via `qwen` login, or API via QWEN_OPENAI_API_KEY. Run `cakit configure qwen`.
# QWEN_OPENAI_API_KEY=
# QWEN_OPENAI_BASE_URL=
# QWEN_OPENAI_MODEL=
# QWEN_MODEL=
# TAVILY_API_KEY=
# GOOGLE_API_KEY=
# GOOGLE_SEARCH_ENGINE_ID=

# OpenHands (API only)
# Config: API only via OPENHANDS_LLM_API_KEY (aliases supported). No OAuth.
# OPENHANDS_LLM_API_KEY=
# OPENHANDS_LLM_MODEL=
# OPENHANDS_LLM_BASE_URL=
# LLM_API_KEY=        # alias for OPENHANDS_LLM_API_KEY
# LLM_MODEL=          # alias for OPENHANDS_LLM_MODEL
# LLM_BASE_URL=       # alias for OPENHANDS_LLM_BASE_URL

# SWE-agent (API only)
# Config: API only via SWE_AGENT_API_KEY/SWE_AGENT_API_BASE/SWE_AGENT_MODEL.
# SWE_AGENT_API_KEY=
# SWE_AGENT_API_BASE=
# SWE_AGENT_MODEL=
# SWE_AGENT_VERSION=
# GITHUB_TOKEN=

# Trae OSS (API only)
# Config: API only via TRAE_AGENT_API_KEY/TRAE_AGENT_API_BASE/TRAE_AGENT_MODEL.
# TRAE_AGENT_API_KEY=
# TRAE_AGENT_API_BASE=
# TRAE_AGENT_MODEL=
# TRAE_AGENT_COMMIT=

# Cursor Agent
# Config: OAuth via `cursor-agent login`, or API via CURSOR_API_KEY.
# CURSOR_API_KEY=
# CURSOR_API_BASE=
# CURSOR_MODEL=

# GitHub Copilot CLI
# Config: OAuth via `copilot` then `/login`, or API via GH_TOKEN/GITHUB_TOKEN.
# GH_TOKEN=        # requires Copilot Requests permission
# GITHUB_TOKEN=    # requires Copilot Requests permission
